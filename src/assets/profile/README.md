# Profile Photo Location

Place your profile photo here, for example:
- `src/assets/profile/vishal.jpg` (or `.png` / `.webp`)

Then update `profileImage` in `src/data/profile.js` to point to your photo file.
